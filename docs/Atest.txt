查看后端日志：
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload

启动前端：
cd fronted 
npm run dev

查看后端端口是否正常运行：
netstat -ano | findstr ":8000"

播放器显示的时长与播放列表显示的时长还是不一致
我已从前端上传视频，你来跟踪日志，检查流程是否按照优化后的进行执行

问题：
如果要处理的视频在本地，应用程序也安装在本地，导入和导出是否可以优化，
将这个问题记录下来，归档，

- 使用 PyAV 或 cv2 替代 subprocess 调用 ffmpeg，减少进程创建开销
- 启用硬链接而非复制大文件
- 分块处理超长视频，边处理边输出