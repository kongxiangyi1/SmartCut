{
  "mode": "funclip",
  "funclip_sub_mode": "merged",
  "ab_test_ratio": 0.1
}
/* 参考文档，此文件用不到，仅用于说明
====================================================
FunClip 流水线模式配置说明
====================================================

【mode 主模式选项】
  - "funclip": FunClip风格的AI处理流水线（推荐使用）
  - "legacy": 旧版流水线（保留兼容）
  - "ab_test": A/B测试模式（同时运行两种流水线进行对比）

【funclip_sub_mode FunClip子模式选项】
  - "merged": 合并方案（当前配置）
    → 单次LLM调用完成所有任务（边界识别 + 评分 + 标题生成）
    → 优点：速度快，Token消耗少
    → 缺点：长视频处理效果可能不稳定
    → 【长视频自动切换】：满足以下任一条件会自动切换到 three_step 模式
       · 字幕条目超过 600 条
       · 视频时长超过 7200 秒（2小时）

  - "three_step": 三步方案
    → 步骤1：边界识别 → 步骤2：片段评分 → 步骤3：标题生成
    → 优点：处理效果稳定，长视频表现更好，支持检查点和降级
    → 缺点：Token消耗稍多，耗时稍长
    → 适用场景：长视频（建议视频 > 2小时或复杂内容时使用此模式）

【ab_test_ratio A/B测试比例】
  - 范围: 0.0 ~ 1.0
  - 仅在 mode="ab_test" 时生效
  - 例如: 0.1 表示10%的流量使用新方案，90%使用旧方案

【长视频检测阈值（代码中定义）】
  - 字幕条目阈值: LONG_VIDEO_SRT_ENTRY_THRESHOLD = 600
  - 时长阈值: LONG_VIDEO_DURATION_SECONDS = 7200 (2小时)
  - 位置: backend/pipeline/topic_postprocess.py
*/
