请为以下内容生成吸引人的标题：

要求：
1. 标题必须包含片段中的具体内容关键词，不能套用通用模板
2. 避免"精彩片段""精彩瞬间""不容错过"等无信息量的空洞标题
3. 长度控制在15字以内，简洁有力

**输出格式要求：**
1. 只输出JSON对象，不要任何解释说明
2. JSON格式：键为片段ID，值为生成的标题
3. 必须为每个片段都生成标题，不要遗漏

**JSON格式示例：**
```json
{"1": "Docker多阶段构建优化技巧", "2": "选择合适的基础镜像"}
```

**输入数据：**
{content}